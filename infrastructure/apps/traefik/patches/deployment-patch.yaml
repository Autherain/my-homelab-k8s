apiVersion: apps/v1
kind: Deployment
metadata:
spec:
  template:
    spec:
      containers:
        - name: traefik
          env:
            # Add custom environment variables
            - name: LOG_LEVEL
              value: "INFO"
            - name: ACCESS_LOG
              value: "true"
            # Example: Add custom configuration
            - name: TRAEFIK_API_DASHBOARD
              value: "true"
