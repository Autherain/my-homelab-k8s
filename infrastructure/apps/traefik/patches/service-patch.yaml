apiVersion: v1
kind: Service
metadata:
  annotations:
    # Add custom annotations to the Traefik service
    prometheus.io/scrape: "true"
    prometheus.io/port: "9000"
    # Example: Add load balancer annotations
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
